@{



var baseUrl = string.Format( "{0}://{1}{2}", Request.Url.Scheme, 
	Request.Url.Authority, Url.Content( "~" ) );
	
//var request = HttpContext.Current.Request
}
<div id="my-carousel" class="carousel slide" data-ride="carousel">
    <div class="carousel-inner">
        <div class="item active">
            <img alt="First slide" src="@Url.Content(string.Format("{0}/images/banner-biorefinery.jpg", baseUrl ) )">
            <div class="carousel-caption">
                <h3></h3>
                <p></p>
            </div>
        </div>
        <div class="item">
	        <img alt="Second slide"
	             src="@Url.Content(string.Format("{0}/images/banner-hay.jpg", baseUrl ) )">
            <div class="carousel-caption">
                <h3></h3>
                <p></p>
            </div>
        </div>
        <div class="item">
	        <img alt="Third slide"
	             src="@Url.Content(string.Format("{0}/images/banner-woody-crop.jpg", baseUrl ) )">
            <div class="carousel-caption">
                <h3></h3>
                <p></p>
            </div>
        </div>
    </div>
</div>

